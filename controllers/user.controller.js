const controller = {
    getUsers : (req, res) => {
        res.json({
            user: 'Marcos Marcelo Maidana'
        });
    },
    postUser: () => {},
    deleteUser: () => {},
}

export default controller;